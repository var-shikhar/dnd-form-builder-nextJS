/** @type {import('next').NextConfig} */
const nextConfig = {
    // below code is commented because in the terminal it is written that it's not needed now.
    // experimental: {
    //     serverActions: true
    // }
}

module.exports = nextConfig
